---
title: "Диалоги в API NN Agent: чтение переписки и отправка сообщений"
description: "Операции NN Agent для работы с перепиской по API: список диалогов аккаунта, история сообщений с собеседником и отправка нового сообщения."
---

Диалог адресуется парой «аккаунт и собеседник»: `account_id` в формате UUID и `peer_user_id` — числовой идентификатор собеседника в мессенджере. В этой же паре читается история и отправляются новые сообщения.

<!-- widget:api -->

## GET /v2/accounts/{account_id}/dialogs

Список диалогов аккаунта.

| Field | Type | Required | Description |
|---|---|---|---|
| `account_id` | string | yes | UUID аккаунта |

## GET /v2/accounts/{account_id}/dialogs/{peer_user_id}/messages

История переписки с конкретным собеседником.

| Field | Type | Required | Description |
|---|---|---|---|
| `account_id` | string | yes | UUID аккаунта |
| `peer_user_id` | integer | yes | Числовой идентификатор собеседника |

## POST /v2/accounts/{account_id}/dialogs/{peer_user_id}/messages

Отправить сообщение собеседнику от имени аккаунта. Тело запроса — JSON.

| Field | Type | Required | Description |
|---|---|---|---|
| `account_id` | string | yes | UUID аккаунта |
| `peer_user_id` | integer | yes | Числовой идентификатор собеседника |

<!-- /widget -->

## Непрочитанные диалоги

Отдельная операция для непрочитанных опубликована только в версии `v1`: `GET /v1/accounts/{account_id}/dialogs/unread`. Полный перечень — [Справочник v1](./v1-reference.md).

## Аналитика по переписке

Диалоги — источник для метрик на вашей стороне: конверсия этапа, доля отказов, качество конкретного сценария. NN Agent отдаёт переписку, считать по ней можно что угодно.

## Дальше

- [Аккаунты](./accounts.md) — где взять `account_id`.
- [Кампании и контакты](./campaigns.md) — как эти диалоги появляются.
- [Ошибки](./errors.md) — что вернётся при неверном идентификаторе.
